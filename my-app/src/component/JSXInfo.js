import React, { Component } from 'react';

class JsxInfo extends Component {
  render() {
    return (
      <div className="JsxInfo">
        <h3>JsxInfo</h3>
        <p>(JSX) is an XML syntax that constructs the markup in React components. React works without JSX, but using JSX makes it easy to read and write the React components as well as structure them just like any other HTML element.</p>
      </div>
    )
  }
}

export default JsxInfo;